name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

# Grant the workflow permission to deploy to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Only allow one deployment at a time; cancel any in-progress run when a new push arrives
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # If GitHub Actions Variables are set, overwrite developer.config.js before packaging.
      # Set variables at: Settings -> Secrets and variables -> Actions -> Variables tab.
      # Fields left unset keep whatever is in developer.config.js in the repo.
      - name: Inject developer config from Actions Variables
        env:
          DEV_NAME:     ${{ vars.DEV_NAME }}
          DEV_TITLE:    ${{ vars.DEV_TITLE }}
          DEV_BIO:      ${{ vars.DEV_BIO }}
          DEV_EMAIL:    ${{ vars.DEV_EMAIL }}
          DEV_PHONE:    ${{ vars.DEV_PHONE }}
          DEV_GITHUB:   ${{ vars.DEV_GITHUB }}
          DEV_LINKEDIN: ${{ vars.DEV_LINKEDIN }}
        run: |
          # Only regenerate if at least one variable is populated
          if [ -n "$DEV_NAME$DEV_EMAIL$DEV_PHONE" ]; then
            cat > developer.config.js << ENDOFCONFIG
          'use strict';
          const DEVELOPER_CONFIG = {
            name:       '${DEV_NAME}',
            title:      '${DEV_TITLE}',
            bio:        '${DEV_BIO}',
            avatarUrl:  '',
            email:      '${DEV_EMAIL}',
            phone:      '${DEV_PHONE}',
            github:     '${DEV_GITHUB}',
            linkedin:   '${DEV_LINKEDIN}',
            skills:     [],
            projects:   [],
            experience: [],
          };
          ENDOFCONFIG
          fi

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      # Upload the entire repo root as the site (no build step needed for static HTML/CSS/JS)
      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
